# JW PowerCache 배포 로드맵

## 현재 상태: 0.4.0 일반 릴리스

현재 목표는 **G7 자체 업데이트에서 조회·설치되는 오픈소스 일반 릴리스**입니다. 베타 접미사를 제거하되 기존 지원 범위와 정합성 제한은 유지합니다. 절대 정합성이나 전체 사이트 캐시를 선전하지 않습니다.

### 일반 릴리스 배포 검증 조건

- PHP 8.2/8.5, Redis, SQLite, MySQL, MariaDB CI 통과
- 제어 키 유실, Redis 재시작, 롤백, 동일 트랜잭션 무효화 테스트 통과
- 고비용 공개 경로에서 동시성 1/4/16/32 기준 p95·처리량 20% 이상 개선, 오류율 0.1% 이하
- 15분 대표 동시성 장애 캠페인에서 오래된 응답·개인화 응답 0건
- 클린 설치, 이전 버전 롤백, 최신 버전 업데이트, 비활성/재활성 검증
- 보안 신고, 지원 범위, 기여 규칙, 변경 이력, 체크섬이 포함된 배포 문서

정확한 릴리스 커밋의 태그·체크섬·빌드 출처 증명을 공개하고, GitHub Latest 조회와 G7 자체 업데이트 경로를 검증합니다. 일반 릴리스 전환은 아래 1.0 보증 게이트의 통과를 의미하지 않습니다.

## 1.0 안정판 검증 조건

- 동일 트랜잭션 데이터 변경 훅이 포함된 공식 G7 버전 또는 명시적으로 버전 관리되는 지원 G7 패키지 확정
- 최종 지원 G7에서 CI, 설치·업그레이드·롤백, 장애 캠페인 재실행
- 관리자 설정의 권한, 저장, 재로드, 실행 모드 반영을 실제 브라우저에서 확인
- 백업 복구와 릴리스 롤백 연습 통과
- 서명된 릴리스 노트, SHA-256 체크섬, 빌드 출처 증명 공개

관리자 브라우저 검증, 실제 백업 복구, `alpha.3 → alpha.2 → alpha.3` 릴리스 롤백은 현재 고정 G7 커밋에서 통과했습니다. 남은 1.0 조건은 공식 G7 릴리스 또는 별도 배포 가능한 지원 G7 패키지 확정과 태그 기반 빌드 출처 증명 공개입니다.

고정된 `5개 사이트·30일` 조건은 두지 않습니다. 실사이트 운영 검증은 최초 유료 SLA 제공 전, 비핵심 파일럿 사이트에서 배포 규모와 위험에 맞게 수행합니다.

## 상용 제공 게이트

상용화는 코드 품질과 별개의 운영 상품 계약입니다.

- 1.0 안정판 배포 파일 기반 설치·업데이트·롤백 지원
- 지원 G7/PHP/Redis 버전과 제외 범위 명시
- 장애 대응 수준, 지원 기간, 유상 업데이트 조건을 별도 계약으로 정의

JW PowerCache는 공개 API와 사용자별로 격리된 게시판 목록의 보수적 허용목록 캐시입니다. 그 외 인증·권한·개인화 응답은 상용 범위에서도 캐시하지 않습니다.
